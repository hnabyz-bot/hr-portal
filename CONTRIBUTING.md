# 기여 안내

hr-portal에 변경을 넣는 방법입니다. **개발 경험이 없어도 따라올 수 있게** 썼습니다.

배포 절차는 [DEPLOY.md](DEPLOY.md), 화면 조작 위주의 설명은 [docs/beginner-guide.md](docs/beginner-guide.md)에 있습니다.

---

## 0. 가장 먼저 — 이것만은 하지 마세요

| 하지 말 것 | 왜 |
|---|---|
| **API 키·비밀번호를 커밋** | 이 저장소는 **Public**입니다. 지워도 히스토리에 남고, 네이버 지도 API는 사용량 과금이 있어 실제 금전 피해로 이어집니다 |
| **규정 원문(PDF/DOCX) 커밋** | 사내 규정이 전 세계에 공개됩니다. 서버의 `regulations/` 폴더에만 둡니다 |
| **실제 직원 개인정보 입력** | 표시용 데이터는 전부 가상 값만 씁니다 |
| **사내 IP·서버 호스트명 기재** | 이슈·PR·커밋 어디에도 쓰지 않습니다. "포털 서버", "경비 산출기 PC" 같은 **역할 명칭**으로만 표현합니다 |
| **`main` / `release` 직접 push** | 브랜치 보호 규칙으로 막혀 있습니다. 모든 변경은 PR을 거칩니다 |

키를 전달해야 한다면 **직접 입력 → USB → 사내 보안 채널 → 구두 전달** 순으로 선택하고, 완료 보고는 **값 없이 체크 형태로만** 합니다.

---

## 1. 작업 흐름

```
브랜치 생성 → 수정 → 로컬 확인 → 커밋/푸시
  → PR 생성 → 자동 검사(CI) 통과 → merge → 브랜치 삭제
  → release 자동 갱신 → 서버 반영 (2분 이내)
```

### 브랜치

**기능 하나 = 브랜치 하나 = PR 하나.** 여러 기능을 한 PR에 섞지 않습니다.

| 목적 | 이름 예시 |
|---|---|
| 새 기능 | `feature/notice-board` |
| 버그 수정 | `fix/expense-search-error` |
| 문서만 | `docs/deploy-guide` |

```bash
git switch -c feature/짧은-설명 origin/main
```

**merge된 브랜치는 바로 삭제합니다.** 안 지우면 쌓여서 어느 게 살아있는지 알 수 없게 됩니다.

### 커밋 메시지

한 줄 요약 + 빈 줄 + 이유. **무엇을 했는지보다 왜 했는지**가 중요합니다. 무엇을 했는지는 코드를 보면 알 수 있지만, 왜 했는지는 적어두지 않으면 아무도 모릅니다.

```
경비 산출기: 연비 기준 변경 (1800cc 미만 9->7, 이상 8->6 km/L)

실제 차량 연비가 기준보다 높게 잡혀 있어 유류비가 과소 계산됐다.
사내 기준표에 맞춰 낮춘다.
```

### PR

PR을 열면 템플릿이 자동으로 뜹니다. **빈 칸으로 두지 마세요** — 해당 없으면 "해당 없음"이라고 적습니다.

체크리스트는 **실제로 확인한 것만** 체크합니다. 안 한 것을 체크하면 체크리스트 전체가 무의미해집니다.

---

## 2. 자동 검사(CI)

PR을 열면 5가지 검사가 자동으로 돕니다. **전부 통과해야 merge할 수 있습니다.**

| 검사 | 무엇을 보나 |
|---|---|
| 코드 검사 | 한글 인코딩, HTML 구조, 시크릿, 개인정보 패턴, `public/` 노출 범위 |
| 사이트 동작 확인 | 컨테이너를 실제로 띄워 응답하는지 |
| 평가·계약 도메인 검사 | 업무 규칙(쿼터·등급·KPI·계약) 자체 검증 |
| SQLite 기능 검증 | 설계 전제가 실제로 성립하는지 |
| DB 스키마 검사 | 스키마 적용 + 제약조건이 잘못된 데이터를 거부하는지 |

실패하면 원인을 그대로 알려줍니다. **빨간불이 뜨는 것은 사고가 아니라 검사가 제 일을 한 것입니다.**

로컬에서 미리 돌려볼 수 있습니다.

```bash
bash .ci/check-encoding.sh
bash .ci/check-secrets.sh
bash .ci/check-pii.sh
bash .ci/check-public.sh
python3 .ci/check-html.py

cd backend
python3 -m hr_eval.selfcheck              # 업무 규칙
python3 -m hr_eval.sql.check_schema       # DB 제약조건
python3 -m hr_eval.sql.check_sqlite_features
```

---

## 3. 로컬에서 확인하기

**화면만 바꿨다면** — `public/` 안에서 간단한 웹 서버를 띄웁니다.

```bash
cd public && python3 -m http.server 8000
```

브라우저에서 `http://127.0.0.1:8000` 으로 엽니다. **파일을 더블클릭해서 여는 방식(`file://`)은 쓰지 마세요** — 일부 기능이 동작하지 않아 확인이 되지 않습니다.

**백엔드(`backend/`)를 바꿨다면** — 포털 서버에서 컨테이너를 다시 빌드해야 합니다. [DEPLOY.md](DEPLOY.md) 참고.

---

## 4. 저장소 구조

| 경로 | 내용 |
|---|---|
| `public/index.html` | 화면 전체 (단일 파일, 인라인 CSS/JS) |
| `backend/` | Flask 앱 — 경비 계산, 규정 검색, 평가·계약 도메인 |
| `backend/hr_eval/domain/` | 업무 규칙 (쿼터·등급·KPI·계약). **DB에 의존하지 않습니다** |
| `backend/hr_eval/sql/` | DB 스키마와 제약조건 검사 |
| `.ci/` | 자동 검사 스크립트 |
| `.github/workflows/ci.yml` | CI 정의 |
| `docker-compose.yml` · `nginx.conf` | 컨테이너·프록시 설정 |
| `regulations/` | 규정 원문 자리 (**서버에만 존재**, 커밋되지 않음) |

---

## 5. 새 기능을 만들기 전에

**정의(한 문장) + 화면 구성 + 저장할 정보 + 지켜야 할 규칙**을 먼저 확정합니다. **규칙이 빠지면 만들지 않습니다.**

"일단 만들어보고 나중에 고치자"가 가장 비쌉니다. 특히 인사 데이터는 한 번 잘못 설계하면 되돌리기 어렵습니다.

구현 순서는 [README.md](README.md) 로드맵을 따릅니다. 쉬운 것부터, 순서를 건너뛰지 않습니다.

---

## 6. 막혔을 때

- **미완성 PR을 여는 것은 문제가 아닙니다.** 잘못된 내용을 확인 없이 merge하는 것이 문제입니다.
- CI가 실패하면 오류 메시지를 **그대로** 이슈나 PR에 붙여주세요. 요약하지 말고 그대로가 가장 빠릅니다.
- 작업 규칙 전반: [이슈 #11](https://github.com/hnabyz-bot/hr-portal/issues/11)
