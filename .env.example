# 이 파일을 복사해서 같은 위치에 ".env" 로 저장하고 실제 키 값을 채운다.
# .env 파일은 git에 올리지 않는다 (.gitignore 처리됨) — 서버에 직접 생성해야 한다.
#
# secom-allowance-calculator/project/.env 에 있는 값과 동일하게 넣으면 된다.

NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
NAVER_LOCAL_CLIENT_ID=
NAVER_LOCAL_CLIENT_SECRET=
OPINET_API_KEY=
